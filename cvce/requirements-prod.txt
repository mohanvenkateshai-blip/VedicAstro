# Production runtime deps (lean). Excludes jyotishganit/skyfield, which are only
# used by the dev-time /cross-validate cross-check (golden suite uses the full
# requirements.txt). PyJHora bundles the Swiss Ephemeris data files.
pyswisseph>=2.10.03
PyJHora>=4.8.7
fastapi>=0.115.0
uvicorn>=0.30.0
numpy>=2.0
pandas>=2.0
pytz
geocoder
geopy>=2.4.0
timezonefinder>=8.0
